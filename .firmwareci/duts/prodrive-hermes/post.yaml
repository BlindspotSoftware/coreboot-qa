post-stage:
  - cmd: newdutctl
    name: Clean up. Shutdown the DUT.
    parameters:
      agent: "[[attributes.Agent]]"
      device: "[[attributes.Device]]"
      command: power
      args: ["off"]
